# DATA SOURCE - MySQL (development)
spring.datasource.url=jdbc:mysql://localhost:3306/nextgen_healthcare?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=@Razaq1986
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# H2 console - keep enabled if you use H2 for tests
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# Server port
server.port=8080
server.address=0.0.0.0
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.boot.autoconfigure.web.servlet=DEBUG

# AI Service Configuration
ai.service.url=http://localhost:5000
